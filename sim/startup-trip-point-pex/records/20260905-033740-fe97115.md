# Record 20260905-033740-fe97115

- **Experiment**: startup-trip-point-pex (issue #14)
- **Claim**: sg13g2-bandgap's bandgap_startup circuit, re-simulated
  with XMSENSE/XMKFB geometry taken from the routed
  `layout/bandgap_startup/bandgap_startup.gds` extraction (real
  drawn W/L/AS/AD/PS/PD, plus real Metal1/Metal2 wire R/C, issue #37)
  instead of the current schematic's, engages
  at cold start and disengages once the core is running, across the
  full PVT grid -- **AS DRAWN**, which is now w=10u for XMSENSE,
  matching the schematic's current w=10u (issue #32 regenerated the
  layout to match decision record 0003's earlier schematic-level
  fix -- see this directory's README.md for the before/after
  account). This direct sns1-sweep alone reads PASS at every point
  below (it always completes release by sns1=vdd) -- as it also did
  at the stale w=2u width, so this number alone does not confirm the
  fix -- but a cross-bench comparison against core-open-loop-bias-pex's
  own real sns1 operating point, which previously reproduced decision
  record 0003's exact same 4 flagged points (wcs_125c_*,
  sf_125c_3.63v) at the stale w=2u width, now clears at all 45
  points -- see this directory's README.md 'Cross-bench observation'
  for the numbers. Post-layout (PEX) evidence for issue #14/#32, NOT
  a claim against any ratified spec row (none is ratified yet -- see
  #125).
- **Devices**: XMSENSE/XMKFB -- sg13_hv_nmos, geometry from
  `layout/bandgap_startup/bandgap_startup.pex.spice`
  (`klt extract --deck sg13g2 --parasitics`, layout git sha
  `7b4d0e1`), body tied to vss as a testbench fixture
  (as of issue #176/#158, the extraction now reports a REAL
  per-terminal hub leg on `VSS` for each body terminal, not the
  deck's synthesized `vsubs` fallback an earlier revision of this
  record described -- this testbench still ties body directly to
  the ideal `vss` net rather than modelling that hub leg, a
  documented simplification, not an unverifiable fixture anymore).
  XRPU (rhigh) is now instantiated from the extracted R$3 device
  (issue #59) instead of spliced from the schematic -- real drawn
  geometry via an X-subckt call to the real rhigh PDK subckt. As of
  issue #176/#158, its "a" terminal wires to the real `vdd__t0`
  hub (the isolated $5 net an earlier revision of this record
  described no longer exists -- this layout now declares a real
  `vdd` pin), and its bulk terminal ties directly to `vss`
  (dropped its own real hub leg, same simplification as the nfet
  bodies above). See README.md for the current, more nuanced
  picture.
- **PDK**: `ihp-sg13g2` at `/home/ubuntu/share/pdk` -- pinned release: see
  `sim/pdk.json`.
- **OSDI models**: `/home/ubuntu/share/pdk/ihp-sg13g2/libs.tech/ngspice/osdi` -- built by `sim/tools/build-osdi.sh`;
  compiler provenance pinned in `sim/pdk.json` ("osdi_toolchain").
- **ngspice**: `** ngspice-46 : Circuit level simulation program`
- **Corner matrix run**: process corner {typ, bcs, wcs, sf, fs} (MOS-hv
  x resistor sections) x temperature {-40, 27, 125} C x supply
  {2.97, 3.30, 3.63} V = 45 points.
- **Result**: 45/45 points PASS (all four criteria, same
  as sim/startup-trip-point, not merely a clean ngspice exit).
- **Links**:
  - Template: `testbench/tb_startup_trip_point_pex.spice.tmpl`
  - Per-point generated netlists: `netlist-snapshots/20260905-033740-fe97115/`
  - Per-point raw ngspice logs: `corners/20260905-033740-fe97115/`
  - Parsed CSV: `records/20260905-033740-fe97115.csv`
  - Extraction inputs: `layout/bandgap_startup/bandgap_startup.pex.spice`,
    `layout/bandgap_startup/pex_extract_report.json`
- **Timestamp / author**: 2026-09-05T03:37:45Z, Loom Builder
  (agent), issue #14.
