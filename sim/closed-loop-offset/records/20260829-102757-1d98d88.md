# Record 20260829-102757-1d98d88

- **Experiment**: closed-loop-offset
- **Claim**: NOT a Monte Carlo / statistical mismatch claim (out of
  scope -- see README). Through the SAME co-simulated closed-loop
  topology sim/closed-loop-vref-pvt uses (bandgap_core +
  bandgap_amp + bandgap_startup, wired exactly as
  design/bandgap_top.sch specifies, loop NOT broken), a
  deterministic DC offset source Vos is inserted in series between
  the shared sns1 node and bandgap_amp's own inverting input
  (in_n/XMP2 gate only), modeling an amplifier input-referred
  offset. Sweeping Vos in {0, +5mV, -5mV} at three representative
  PVT points (NOT the full 45-point grid -- see README "Scope")
  and comparing the resulting vref measures this design's own
  vref SENSITIVITY to an amplifier input offset (dVref/dVos), not
  this amplifier's actual real offset (unknown without Monte Carlo
  mismatch data -- tracked separately, #4 checklist item 6, N/A
  per #5).
- **Sensitivity summary (dVref/dVos, this run)**:
  - bcs: dVref/dVos=6.5989 V/V (vref: Vos=0 1.038592 V, +Vos 1.071504 V, -Vos 1.005515 V)
  - wcs: dVref/dVos=6.6154 V/V (vref: Vos=0 1.047819 V, +Vos 1.080749 V, -Vos 1.014595 V)
  - typ: dVref/dVos=6.6086 V/V (vref: Vos=0 1.045111 V, +Vos 1.078041 V, -Vos 1.011955 V)
- **XMSENSE width this run used**: w=10u (read from the live
  design/netlist/bandgap_startup.spice at run time).
- **Devices**: all real PDK compact models, all three DUTs copied
  verbatim from design/netlist/bandgap_core.spice,
  design/netlist/bandgap_amp.spice and
  design/netlist/bandgap_startup.spice, loop NOT broken, plus the
  Vos offset-probe fixture documented in testbench/ and README.md.
  No fixture stands in for any real device -- only the offset
  probe is added, same discipline as every closed-loop experiment
  in this tree.
- **Netlist provenance**: schematic
  (design/netlist/bandgap_core.spice @ `8bb15bc`,
  design/netlist/bandgap_amp.spice @ `397fe2d`,
  design/netlist/bandgap_startup.spice @ `66ac99d`),
  device-for-device, wired exactly as design/bandgap_top.sch
  specifies except for the deliberate sns1/sns1_amp offset probe.
- **Nodeset seed provenance**: `sim/closed-loop-startup/records/20260829-102626-1d98d88.csv`
  @ `` (sim/closed-loop-startup's own most recent
  committed record) -- used as a Newton-Raphson initial-guess hint
  only, not an acceptance criterion here (see README "Pass/fail
  criteria").
- **PDK**: `ihp-sg13g2` at `/home/ubuntu/share/pdk` -- pinned release: see
  `sim/pdk.json`.
- **OSDI models**: `/home/ubuntu/share/pdk/ihp-sg13g2/libs.tech/ngspice/osdi` -- built by `sim/tools/build-osdi.sh`;
  compiler provenance pinned in `sim/pdk.json` ("osdi_toolchain").
- **ngspice**: `** ngspice-46 : Circuit level simulation program`
- **Corner matrix run**: 3 representative PVT points (typ/27C/3.30V,
  wcs/-40C/2.97V, bcs/125C/3.63V -- NOT the full 45-point grid,
  deliberately -- see README "Scope: three points, not a PVT
  sweep") x 3 deterministic Vos probe values (0, +5mV, -5mV) =
  9 points.
- **Result**: 9/9 points PASS (.op converged, the
  loop is genuinely closed around its own equilibrium
  (|sns2-sns1_amp| <= 0.02 V), and vref landed in a
  plausible in-range band -- NOT a claim about a target offset or
  mismatch magnitude; see README "What this testbench claims, and
  what it does not").
- **Links**:
  - Template: `testbench/tb_closed_loop_offset.spice.tmpl`
  - Per-point generated netlists: `netlist-snapshots/20260829-102757-1d98d88/`
  - Per-point raw ngspice logs: `corners/20260829-102757-1d98d88/`
  - Parsed CSV: `records/20260829-102757-1d98d88.csv`
- **Timestamp / author**: 2026-08-29T10:27:58Z, Loom Builder
  (agent), issue #88.

## Comparison to superseded record (issue #141)

The previous newest record,
[`records/20260826-114923-874c585.md`](20260826-114923-874c585.md), was
generated against `bandgap_core`'s pre-#134 `R1`. This experiment's own
`testbench/tb_closed_loop_offset.spice.tmpl` had never been updated for
issue #134's committed retune (PR #136: `R1` `rppd l=694.5u -> l=511u`)
even though the co-simulated core section was otherwise copied
verbatim — this record's own template fix (`l=694.5u -> l=511u` on the
`XR1` line) is part of this issue's change, not just a re-run.

The offset-sensitivity gain `dVref/dVos` shifts **materially**, not just
`vref`'s absolute level:

| corner | Superseded `dVref/dVos` (`l=694.5u`) | This record (`l=511u`, current) | Delta |
|---|---|---|---|
| `bcs` | 8.7765 V/V | 6.5989 V/V | **-24.81%** |
| `wcs` | 8.7996 V/V | 6.6154 V/V | **-24.82%** |
| `typ` | 8.7902 V/V | 6.6086 V/V | **-24.82%** |

This tracks README.md's own first-order model,
`dVref/dVos ~= 1 + R1/R2`: the drawn `R1/R2` ratio drops from
`694.5/82.7 = 8.398` to `511/82.7 = 6.179` with issue #134's retune, a
`-26.4%` shift in the ratio, consistent (within the model's own
first-order approximation) with the `~-24.8%` measured shift in the
sensitivity gain across all three corners. This is the expected,
mechanical consequence of retuning `R1` (the single degree of freedom
this topology has, per
`sim/closed-loop-vref-pvt/139-post-retune-evidence-refresh.md`'s
tradeoff writeup) — not a testbench regression. The design still passes
the same 9/9-point convergence/loop-closure criteria; this record is not
a claim against any ratified offset-sensitivity spec target (none
exists — see the record's own "Claim" bullet).
