# Record 20260829-085121-94869af

- **Experiment**: sg13cmos5l-closed-loop-startup-pex (issue #84)
- **Claim**: co-simulating SG13CMOS5L's bandgap_core + bandgap_amp +
  bandgap_startup with every MOS device's geometry (w/l + real drawn
  junction as/ad/ps/pd) taken from the routed
  `layout/sg13cmos5l-bandgap_top/sg13cmos5l-bandgap_top.gds`
  extraction instead of the schematic's as-drawn (junction-less)
  defaults, wired exactly as design/sg13cmos5l/bandgap_top.sch
  specifies, with vdd itself ramped 0 -> VDD over 200 us, the
  assembled block still self-starts and settles to a closed-loop
  operating point across the full temperature x supply x
  pnpMPA/MOS/resistor-process-corner PVT grid: the startup circuit
  fully releases (v(det) <= 0.2*vdd, |i(XMKFB)| <=
  50e-9 A), the real error amplifier closes the loop
  (|sns1-sns2| <= 0.020 V), and fb settles to a real
  interior equilibrium rather than railing to either supply (fb
  within [0.05 V, vdd-0.05 V]). This is
  post-layout (PEX) evidence for issue #84 -- see this directory's
  README.md for exactly what is and is not extracted/modelled (no
  wire/metal parasitics -- klt's sg13cmos5l deck's --parasitics
  support is currently broken, klayout-tools#1440; bipolar and
  resistor devices are still schematic-sourced, not extracted --
  klayout-tools#1242/#1415). No ratified spec row exists yet for
  SG13CMOS5L (spec/porting-plan-sg13cmos5l.md, "Status: engineering
  input, not a ratified decision"); this record is closed-loop
  infrastructure/plumbing evidence -- the assembled block starts and
  settles on post-layout geometry -- not a claim against any spec
  accuracy/PSRR/Iq target.
- **Devices**: XM1/XM2/XM3 (core), XMTAIL/XMP1-4/XMN1-4 (amp),
  XMSENSE/XMKFB (startup) -- all 14 MOS devices, geometry from
  `layout/sg13cmos5l-bandgap_top/sg13cmos5l-bandgap_top.pex.spice`
  (`klt extract --deck sg13cmos5l` -- no --parasitics, see
  README.md, layout git sha `0485f7e`), re-encoded as
  X-subckt calls to the real sg13_hv_pmos/sg13_hv_nmos compact
  models. XQ1-XQ3 (pnpMPA) and XR1/XR2 (rppd) still spliced verbatim
  from design/sg13cmos5l/netlist/bandgap_core.spice (schematic git
  sha `a6dcd36`) -- the sg13cmos5l extraction deck does
  not recognise bipolar or resistor devices (klayout-tools#1242,
  #1415). XRPU (rhigh) spliced verbatim from
  design/sg13cmos5l/netlist/bandgap_startup.spice (schematic git sha
  `8361d0d`), same reason. No wire (metal)
  parasitics are modelled anywhere in this netlist -- every net is
  the same idealized zero-impedance node the schematic-level
  sim/sg13cmos5l-closed-loop-startup testbench already uses; see
  README.md for why (klayout-tools#1440).
- **PDK**: `ihp-sg13cmos5l` at `/Users/rwalters/share/pdk` -- pinned revision: see
  `sim/pdk-sg13cmos5l.json` (git commit pin, not a tagged release).
- **OSDI models**: `/Users/rwalters/share/pdk/ihp-sg13cmos5l/libs.tech/ngspice/osdi` -- shipped prebuilt by this PDK
  checkout; see `sim/pdk-sg13cmos5l.json` "osdi_toolchain".
- **ngspice**: `** ngspice-47 : Circuit level simulation program`
- **Corner matrix run**: process corner {typ, bcs, wcs, sf, fs}
  (pnpMPA x MOS-hv x resistor sections -- see
  sim/sg13cmos5l-closed-loop-startup/README.md for the pairing,
  unchanged here) x temperature {-40, 27, 125} C x supply {2.97,
  3.30, 3.63} V = 45 points.
- **Result**: 45/45 points PASS (startup-release,
  loop-closure and not-railed criteria above, not merely a clean
  ngspice exit).
- **Links**:
  - Template: `testbench/tb_sg13cmos5l_closed_loop_startup_pex.spice.tmpl`
  - Per-point generated netlists: `netlist-snapshots/20260829-085121-94869af/`
  - Per-point raw ngspice logs: `corners/20260829-085121-94869af/`
  - Parsed CSV: `records/20260829-085121-94869af.csv`
  - Extraction inputs:
    `layout/sg13cmos5l-bandgap_top/sg13cmos5l-bandgap_top.pex.spice`,
    `layout/sg13cmos5l-bandgap_top/pex_extract_report.json`
- **Timestamp / author**: 2026-08-29T08:52:00Z, Loom Builder
  (agent), issue #84.
