# Record 20260829-103823-1d98d88

- **Experiment**: core-open-loop-bias-pex (issue #14)
- **Claim**: sg13g2-bandgap's bandgap_core, re-simulated with its
  PMOS mirror legs' geometry taken from the routed
  `layout/bandgap_core/bandgap_core.gds` extraction (real drawn
  W/L/AS/AD/PS/PD, no ideal-primitive substitutions) instead of the
  schematic's as-drawn defaults, still produces a PTAT delta-VBE and
  a summed CTAT+PTAT vref across the full PVT grid. This is
  post-layout (PEX) evidence for issue #14 -- see this directory's
  README.md for exactly what is and is not extracted/modelled
  (bipolar devices are still schematic-sourced, not extracted; the
  rppd resistors ARE extracted as of issue #59; Metal1/Metal2 wire
  R/C are now modelled, issue #37; PMOS body is a
  testbench fixture, not a real extracted tie). NOT a claim against
  any ratified spec row (none is ratified yet -- see #13).
- **Devices**: XM1/XM2/XM3 -- sg13_hv_pmos, geometry (+ wire R/C,
  issue #37) from `layout/bandgap_core/bandgap_core.pex.spice`
  (`klt extract --deck sg13g2 --parasitics`, layout git sha
  `acf3713`). XQ1-XQ3 (npn13G2) still spliced verbatim
  from `design/netlist/bandgap_core.spice` (schematic git sha
  `8bb15bc`) -- the sg13g2 extraction deck still does not
  recognise bipolar devices (klayout-tools, filed generically).
  XR1/XR2 (rppd) are now instantiated from the extracted R$5/R$4
  devices (issue #59) instead of spliced from the schematic -- real
  drawn geometry via an X-subckt call to the real rppd PDK subckt,
  bulk on vsubs rather than the schematic's sub!. See README.md and
  layout/README.md for the current, more nuanced picture.
- **PDK**: `ihp-sg13g2` at `/home/ubuntu/share/pdk` -- pinned release: see
  `sim/pdk.json` (IHP-Open-PDK v0.3.0 as fetched by this repo's
  convention; re-verify PDK_ROOT actually resolves to that release
  before trusting this record on a different machine).
- **OSDI models**: `/home/ubuntu/share/pdk/ihp-sg13g2/libs.tech/ngspice/osdi` -- built by `sim/tools/build-osdi.sh`
  from the PDK's own Verilog-A sources; compiler provenance is pinned
  in `sim/pdk.json` ("osdi_toolchain").
- **ngspice**: `** ngspice-46 : Circuit level simulation program`
- **Corner matrix run**: process corner {typ, bcs, wcs, sf, fs} (HBT x
  MOS-hv x resistor sections -- see sim/core-open-loop-bias/README.md
  for the pairing, unchanged here) x temperature {-40, 27, 125} C x
  supply {2.97, 3.30, 3.63} V = 45 points.
- **Result**: 45/45 points PASS (ngspice exit 0, no model-load
  error in the log, and all probed node voltages and leg currents
  present).
- **Links**:
  - Template: `testbench/tb_core_open_loop_bias_pex.spice.tmpl`
  - Per-point generated netlists: `netlist-snapshots/20260829-103823-1d98d88/`
  - Per-point raw ngspice logs: `corners/20260829-103823-1d98d88/`
  - Parsed CSV: `records/20260829-103823-1d98d88.csv`
  - Extraction inputs: `layout/bandgap_core/bandgap_core.pex.spice`,
    `layout/bandgap_core/pex_extract_report.json`
- **Timestamp / author**: 2026-08-29T10:38:27Z, Loom Builder
  (agent), issue #14.

## Comparison to superseded record (issue #141)

The previous newest record,
[`records/20260825-145023-2ff6265.md`](20260825-145023-2ff6265.md), was
generated against a template that hardcoded `bandgap_core`'s pre-#134
`R1` value (`XR1 ... rppd w=2u l=694.5u`) on its splice-in line for the
extracted resistor — **not** because the layout itself was stale
(`layout/bandgap_core/pex_extract_report.json`'s `$5` device already
reports `l_um: 511.0` / `r_ohm: 66430.0`, confirming PR #138's layout
resync did correctly propagate issue #134's retune into the extraction),
but because this experiment's own
`testbench/tb_core_open_loop_bias_pex.spice.tmpl` had never been updated
to match. This record's own template fix (`l=694.5u -> l=511u` on the
`XR1` line, plus correcting the template's/README's own stale
`90285`/`89782`-ohm sheet-resistance/compact-model commentary to
`66430`/`66257`) is part of this issue's change, not just a re-run.

At the nominal corner (`typ`/27C/3.30V), comparing the two records' own
CSVs directly:

| Quantity | Superseded (`l=694.5u`) | This record (`l=511u`, current) | Delta |
|---|---|---|---|
| `vref_v` | 1.163992 V | 1.044287 V | **-10.28%** |
| `r1_ohm` (measured, `(vref-vbe_q3)/i_leg3`) | 89.97 kOhm | 66.26 kOhm | -26.36% (tracks the `l_um` ratio `511/694.5 = 73.6%` from `pex_extract_report.json`, minus the small compact-model end/contact correction the template's own commentary documents) |
| `dvbe_ptat_v` / `vbe_q1_v` / `vbe_q2_v` / `i_leg1_a` / `i_leg2_a` / `r2_ohm` | unchanged | unchanged | unchanged (`R1` does not appear in the `Q1`/`Q2` PTAT leg or in `R2`) |
| `vref_v` across the 45-point PVT grid | 0.990416 V - 1.292998 V | 0.880418 V - 1.162492 V | shifts down at every point |

This is the same expected, mechanical shift issue #134's retune
produces in every other open-loop record refreshed by this issue (see
e.g. `sim/core-open-loop-bias`'s and
`sim/startup-core-handover`'s own #141 comparison sections) — not a
regression in this PEX testbench, and not a claim against any ratified
spec row (none exists yet; this record remains infrastructure/plumbing
evidence per its own "Claim" bullet above).
