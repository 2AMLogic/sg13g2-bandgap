# Record 20260829-103548-1d98d88

- **Experiment**: startup-core-handover
- **Claim**: co-simulating bandgap_core + bandgap_startup in one
  netlist (sharing the real sns1 and fb nodes, no externally-swept
  stimulus on either), with vdd itself ramped 0 -> VDD over 200 us,
  XMKFB (the startup circuit's fb pull-down) releases the shared fb
  node once the core reaches its ramped-up, settled operating point:
  v(det) <= 0.2*vdd and |i(XMKFB)| <= 50e-9 A
  (1% of the 5 uA/leg open-loop design current) at the end of the
  transient (t=2ms), across the full temperature x supply x
  HBT/MOS/resistor-process-corner PVT grid. Direct, load-bearing
  evidence for issue #24 -- supersedes the cross-bench comparison
  sim/startup-trip-point/README.md documents (two separate DC
  benches, not a co-simulation).
- **XMSENSE width this run used**: w=10u (read from the live
  design/netlist/bandgap_startup.spice at run time, not hardcoded --
  see this experiment's README for why).
- **Devices**: all real PDK compact models, both DUTs copied
  verbatim from design/netlist/bandgap_core.spice (three npn13G2
  legs, three sg13_hv_pmos mirror legs, two rppd resistors) and
  design/netlist/bandgap_startup.spice (rhigh pull-up, two
  sg13_hv_nmos switches).
- **Netlist provenance**: schematic
  (design/netlist/bandgap_core.spice @ `8bb15bc`,
  design/netlist/bandgap_startup.spice @ `66ac99d`),
  device-for-device, plus the open-loop mirror-bias fixture (XM0 +
  Ibias, identical to sim/core-open-loop-bias's own) and the Vmkfb
  ammeter documented in testbench/ and README.md.
- **PDK**: `ihp-sg13g2` at `/home/ubuntu/share/pdk` -- pinned release: see
  `sim/pdk.json`.
- **OSDI models**: `/home/ubuntu/share/pdk/ihp-sg13g2/libs.tech/ngspice/osdi` -- built by `sim/tools/build-osdi.sh`;
  compiler provenance pinned in `sim/pdk.json` ("osdi_toolchain").
- **ngspice**: `** ngspice-46 : Circuit level simulation program`
- **Corner matrix run**: process corner {typ, bcs, wcs, sf, fs}
  (HBT x MOS-hv x resistor sections) x temperature {-40, 27, 125} C
  x supply {2.97, 3.30, 3.63} V = 45 points.
- **Result**: 45/45 points PASS (both the v(det) and
  |i(XMKFB)| release criteria above, not merely a clean ngspice exit).
- **Links**:
  - Template: `testbench/tb_startup_core_handover.spice.tmpl`
  - Per-point generated netlists: `netlist-snapshots/20260829-103548-1d98d88/`
  - Per-point raw ngspice logs: `corners/20260829-103548-1d98d88/`
  - Parsed CSV: `records/20260829-103548-1d98d88.csv`
- **Timestamp / author**: 2026-08-29T10:35:58Z, Loom Builder
  (agent), issue #24.

## Comparison to superseded record (issue #141)

The previous newest record,
[`records/20260821-144329-60f50a7.md`](20260821-144329-60f50a7.md), was
generated against `bandgap_core`'s pre-#134 `R1`. This experiment's own
`testbench/tb_startup_core_handover.spice.tmpl` had never been updated
for issue #134's committed retune (PR #136: `R1` `rppd l=694.5u ->
l=511u`) even though the core section was otherwise copied verbatim —
this record's own template fix (`l=694.5u -> l=511u` on the `XR1` line)
is part of this issue's change, not just a re-run. At the nominal corner
(`typ`/27C/3.30V), comparing the two records' own CSVs directly:

| Quantity | Superseded (`l=694.5u`) | This record (`l=511u`, current) | Delta |
|---|---|---|---|
| `vref_final_v` | 1.16369 V | 1.04383 V | **-10.30%** |
| `vref_final_v` across the 45-point grid | 0.990001 V - 1.29283 V | 0.879874 V - 1.16213 V | shifts down at every point |
| `det_early_v` / `fb_early_v` / `det_final_v` / `fb_final_v` / `sns1_final_v` | unchanged | unchanged | unchanged |
| `i_mkfb_final_a` (release ammeter, ~1e-13 A) | 9.69611e-14 A | 9.70474e-14 A | +0.09%, negligible |

Only `vref` moves materially, and only because `R1` sits directly in its
own leg (`vref = VBE(Q3) + i_leg3*R1`); every startup-release/handover
criterion this testbench actually checks (`det`/`fb`/`i(XMKFB)`) is
unaffected, matching the same result pattern
`sim/core-open-loop-bias` (the sibling open-loop fixture this testbench
shares its mirror-bias approach with) shows in its own #141 refresh.
This is the expected, mechanical consequence of issue #134's retune, not
a regression in this testbench.
