# Record 20260829-102626-1d98d88

- **Experiment**: closed-loop-startup
- **Claim**: co-simulating bandgap_core + bandgap_amp + bandgap_startup
  in one netlist (design/bandgap_top.sch's own wiring: core.fb <-
  amp.out, core.sns1 -> amp.in_n, core.sns2 -> amp.in_p, startup
  shares sns1 and fb with the core), with vdd itself ramped 0 -> VDD
  over 200 us, the assembled block self-starts and settles to a
  closed-loop operating point across the full temperature x supply x
  HBT/MOS/resistor-process-corner PVT grid: the startup circuit fully
  releases (v(det) <= 0.2*vdd, |i(XMKFB)| <=
  50e-9 A), the real error amplifier closes the loop
  (|sns1-sns2| <= 0.020 V), and fb settles to a real
  interior equilibrium rather than railing to either supply (fb
  within [0.05 V, vdd-0.05 V]). This is
  the first genuinely closed-loop testbench in this tree -- every
  prior PVT-sweep testbench (sim/core-open-loop-bias,
  sim/startup-core-handover, and their -pex variants) substituted an
  ideal diode-connected-replica current fixture for the not-yet-built
  amplifier (design/README.md, issue #9's scope cut). No ratified
  spec row exists yet (#13); this record is closed-loop
  infrastructure/plumbing evidence -- the assembled block starts and
  settles -- not a claim against any spec accuracy/PSRR/Iq target.
- **XMSENSE width this run used**: w=10u (read from the live
  design/netlist/bandgap_startup.spice at run time, same convention
  sim/startup-core-handover uses).
- **Devices**: all real PDK compact models, all three DUTs copied
  verbatim from design/netlist/bandgap_core.spice (three npn13G2
  legs, three sg13_hv_pmos mirror legs, two rppd resistors),
  design/netlist/bandgap_amp.spice (five sg13_hv_pmos, four
  sg13_hv_nmos -- the error amplifier issue #58 adds) and
  design/netlist/bandgap_startup.spice (rhigh pull-up, two
  sg13_hv_nmos switches). No fixture stands in for the servo loop
  in this experiment -- see the README's comparison against every
  prior experiment in this tree.
- **Netlist provenance**: schematic
  (design/netlist/bandgap_core.spice @ `8bb15bc`,
  design/netlist/bandgap_amp.spice @ `397fe2d`,
  design/netlist/bandgap_startup.spice @ `66ac99d`),
  device-for-device, wired exactly as design/bandgap_top.sch
  specifies, plus the Vmkfb ammeter documented in testbench/ and
  README.md.
- **PDK**: `ihp-sg13g2` at `/home/ubuntu/share/pdk` -- pinned release: see
  `sim/pdk.json`.
- **OSDI models**: `/home/ubuntu/share/pdk/ihp-sg13g2/libs.tech/ngspice/osdi` -- built by `sim/tools/build-osdi.sh`;
  compiler provenance pinned in `sim/pdk.json` ("osdi_toolchain").
- **ngspice**: `** ngspice-46 : Circuit level simulation program`
- **Corner matrix run**: process corner {typ, bcs, wcs, sf, fs}
  (HBT x MOS-hv x resistor sections) x temperature {-40, 27, 125} C
  x supply {2.97, 3.30, 3.63} V = 45 points.
- **Result**: 45/45 points PASS (startup-release,
  loop-closure and not-railed criteria above, not merely a clean
  ngspice exit).
- **Links**:
  - Template: `testbench/tb_closed_loop_startup.spice.tmpl`
  - Per-point generated netlists: `netlist-snapshots/20260829-102626-1d98d88/`
  - Per-point raw ngspice logs: `corners/20260829-102626-1d98d88/`
  - Parsed CSV: `records/20260829-102626-1d98d88.csv`
- **Timestamp / author**: 2026-08-29T10:26:40Z, Loom Builder
  (agent), issue #58.

## Comparison to superseded record (issue #141)

The previous newest record,
[`records/20260825-145208-403a24f.md`](20260825-145208-403a24f.md), was
generated against `bandgap_core`'s pre-#134 `R1`. This experiment's own
`testbench/tb_closed_loop_startup.spice.tmpl` had never been updated for
issue #134's committed retune (PR #136: `R1` `rppd l=694.5u -> l=511u`)
even though the co-simulated core section was otherwise copied
verbatim from `design/netlist/bandgap_core.spice` — this record's own
template fix (`l=694.5u -> l=511u` on the `XR1` line) is part of this
issue's change, not just a re-run. At the nominal corner (`typ`/27C/3.30V),
comparing the two records' own CSVs directly:

| Quantity | Superseded (`l=694.5u`) | This record (`l=511u`, current) | Delta |
|---|---|---|---|
| `vref_final_v` | 1.16677 V | 1.04615 V | **-10.34%** |
| `fb_final_v` | 2.49452 V | 2.49452 V | unchanged |
| `sns1_final_v` / `sns2_final_v` | 0.708681 V / 0.708440 V | 0.708681 V / 0.708440 V | unchanged |
| `dvsns_final_v` (loop-closure residual) | 0.000241 V | 0.000241 V | unchanged |
| `det_final_v` / `i_mkfb_final_a` (startup-release) | unchanged | unchanged | unchanged |

Only `vref` moves — expected, since `R1` sits only in the `vref` leg
(`vref = VBE(Q3) + i_leg3*R1`) and the amplifier's own loop-closure
behavior (`sns1`/`sns2`/`fb`/startup-release criteria) does not depend on
`R1` at all. All 45/45 points still PASS the same startup-release,
loop-closure and not-railed criteria; this is closed-loop
infrastructure/plumbing evidence, not a claim against any ratified
`vref` accuracy target (see the record's own "Claim" bullet above), so
the `vref` shift does not change this record's pass/fail verdict.
Flagged explicitly per issue #141's acceptance criteria rather than left
for a reader to notice by diffing CSVs.
