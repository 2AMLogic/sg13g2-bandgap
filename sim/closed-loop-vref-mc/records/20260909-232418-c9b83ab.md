# Record 20260909-232418-c9b83ab

- **Experiment**: closed-loop-vref-mc
- **Claim**: through the SAME co-simulated closed-loop topology
  sim/closed-loop-vref-pvt and sim/closed-loop-offset both use
  (bandgap_core + bandgap_amp + bandgap_startup, wired exactly as
  design/bandgap_top.sch specifies, loop NOT broken, real devices
  throughout, no ideal feedback fixture), a device-MISMATCH Monte
  Carlo campaign (T1 tracker #4 checklist item 6, evidence for the
  #150/#128 Output-reference-row escalation): N=300 draws of
  vref's DC operating point per point, seeded
  (`.options rndseed=20260909+draw_index`, draw_index=0..299,
  applied identically at every point below) against SG13G2's own
  agauss()-based statistical device-mismatch corner-lib sections
  (hbt_typ_mismatch/mos_tt_mismatch/res_typ_mismatch at the nominal
  point, and their bcs/wcs siblings at the two required process
  corners), COMBINED WITH (not instead of) the process-corner axis:
  bcs/wcs at -40C and 125C, 3.30V -- the same corner/temp/vdd points
  sim/closed-loop-startup's own committed record already names as
  PASS, reused here as this campaign's .nodeset DC-bias seed.
  PLUS a deterministic NEGATIVE CONTROL: the identical N=300 seeds
  0..299 replayed against the PLAIN (non-mismatch)
  hbt_typ/mos_tt/res_typ sections at the same nominal point --
  confirmed EXACTLY ZERO spread (vref=1.047339 V at every
  one of the 300 negative-control draws; this script hard-fails
  before ever reaching this record if that is not so).
- **XMSENSE width this run used**: w=10u (read from the live
  design/netlist/bandgap_startup.spice at run time, same convention
  every closed-loop experiment in this tree uses).
- **Devices**: all real PDK compact models, all three DUTs copied
  verbatim from design/netlist/bandgap_core.spice,
  design/netlist/bandgap_amp.spice and
  design/netlist/bandgap_startup.spice -- identical device set to
  sim/closed-loop-vref-pvt and sim/closed-loop-offset. No fixture
  stands in for any real device.
- **Netlist provenance**: schematic
  (design/netlist/bandgap_core.spice @ `d83f7c4`,
  design/netlist/bandgap_amp.spice @ `397fe2d`,
  design/netlist/bandgap_startup.spice @ `66ac99d`),
  device-for-device, wired exactly as design/bandgap_top.sch
  specifies.
- **Nodeset seed provenance**: `sim/closed-loop-startup/records/20260830-132425-d83f7c4.csv`
  @ `94207b4` (sim/closed-loop-startup's own most recent
  committed record) -- used as a Newton-Raphson initial-guess hint
  only, not an acceptance criterion (a real mismatch draw is
  expected to converge NEAR, not exactly at, this seed).
- **PDK**: `ihp-sg13g2` at `/Users/rwalters/share/pdk` -- pinned release: see
  `sim/pdk.json`.
- **OSDI models**: `/Users/rwalters/share/pdk/ihp-sg13g2/libs.tech/ngspice/osdi` -- built by `sim/tools/build-osdi.sh`;
  compiler provenance pinned in `sim/pdk.json` ("osdi_toolchain").
- **ngspice**: `** ngspice-46 : Circuit level simulation program`
- **Corner matrix run**: 1 nominal mismatch point (typ/27C/3.30V) +
  1 nominal negative-control point (typ/27C/3.30V, non-mismatch
  sections) + 4 process-corner mismatch points (bcs/wcs x
  -40C/125C, 3.30V) = 6 points, N=300 draws each =
  1800 total .op simulations.
- **Result**: 6/6 points PASS (a point PASSes here
  when ALL 300 of its draws individually converge with the loop
  genuinely closed and not railed -- see
  `sim/lib/pvt_verdict_common.sh`'s pvt_closed_loop_verdict();
  this is a convergence/loop-closure gate, NOT a pass/fail against
  any accuracy target -- see README.md "What this evidence does
  and does not claim").
- **Links**:
  - Template: `testbench/tb_closed_loop_vref_mc.spice.tmpl`
  - Representative (draw index 0) per-point netlists:
    `netlist-snapshots/20260909-232418-c9b83ab/`
  - Representative (draw index 0) per-point raw ngspice logs:
    `corners/20260909-232418-c9b83ab/`
  - Per-point digest CSV (corner-id-matched, per sim/README.md):
    `records/20260909-232418-c9b83ab.csv`
  - Per-draw raw CSV (all 1800 draws, the evidence this
    record's statistics are computed from):
    `records/20260909-232418-c9b83ab-draws.csv`
- **Timestamp / author**: 2026-09-09T23:41:52Z, Loom Builder
  (agent), issue #215.

## Per-point Monte Carlo statistics (informational -- see disclaimer above)

Untrimmed `vref`, N=300 draws/point, mean/sigma/3sigma-over-mean%/
min/max in volts, plus the fraction of draws within +/-1% and
+/-0.5% of THAT POINT'S OWN mean (not of any spec target -- see
README.md). `n_pass` is out of `n_draws`=300.

| point | temp (C) | n_pass/n_draws | mean (V) | sigma (V) | 3sigma/mean (%) | min (V) | max (V) | within +/-1% | within +/-0.5% |
|---|---|---|---|---|---|---|---|---|---|
| typ_mismatch | 27 | 300/300 | 1.046711 | 0.045486 | 13.0368 | 0.885840 | 1.152241 | 0.2067 | 0.1033 |
| typ_negctrl | 27 | 300/300 | 1.047339 | 0.000000 | 0.0000 | 1.047339 | 1.047339 | 1.0000 | 1.0000 |
| bcs_mismatch | -40 | 300/300 | 1.042605 | 0.042405 | 12.2017 | 0.892596 | 1.142710 | 0.2100 | 0.1033 |
| bcs_mismatch | 125 | 300/300 | 1.041041 | 0.050651 | 14.5961 | 0.864700 | 1.159304 | 0.1767 | 0.0967 |
| wcs_mismatch | -40 | 300/300 | 1.048912 | 0.042258 | 12.0861 | 0.899613 | 1.150098 | 0.2000 | 0.1100 |
| wcs_mismatch | 125 | 300/300 | 1.052766 | 0.050339 | 14.3447 | 0.887744 | 1.172178 | 0.1733 | 0.1000 |
